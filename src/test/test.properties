# src/test/resources/test.properties

# Derby 内存数据库配置（用于测试）
test.db.url=jdbc:derby:memory:testdb;create=true
test.db.username=test
test.db.password=test
test.db.driver=org.apache.derby.jdbc.EmbeddedDriver

# 测试配置
test.mode=derby
test.cleanup.enabled=true